# Routing

Routing and model/skill selection references used by Coordinator and pipeline stages.

Key files:

- `compatibility-matrix.md` — coarse host defaults for planning
- `capability-probes.tsv` — machine-readable catalog of capability checks and manual-verification rows
- `file-trigger-table.md` — file-pattern routing hints for Coordinator
