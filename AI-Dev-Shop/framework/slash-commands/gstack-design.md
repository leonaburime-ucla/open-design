# gstack Design Command (/gstack-design)

Use this command to invoke the `gstack-design` skill.

Requested mode/context: $ARGUMENTS

Directive:
1. Read `<AI_DEV_SHOP_ROOT>/skills/gstack-design/SKILL.md`.
2. Select exactly one mode: `consultation`, `shotgun`, `html`, or `review`.
3. Read exactly one matching reference file from `<AI_DEV_SHOP_ROOT>/skills/gstack-design/references/`.
4. Follow the skill's Output and Guardrails sections.
5. This skill is manual/slash-invoked and is not wired into the default pipeline or downstream agent dispatch.
