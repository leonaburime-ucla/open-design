# gstack iOS Command (/gstack-ios)

Use this command to invoke the `gstack-ios` skill.

Requested mode/context: $ARGUMENTS

Directive:
1. Read `<AI_DEV_SHOP_ROOT>/skills/gstack-ios/SKILL.md`.
2. Select exactly one mode: `qa`, `fix`, `design-review`, `clean`, or `sync`.
3. Read exactly one matching reference file from `<AI_DEV_SHOP_ROOT>/skills/gstack-ios/references/`.
4. Follow the skill's Output and Guardrails sections.
5. This skill is manual/slash-invoked and is not wired into the default pipeline or downstream agent dispatch.
