# interface-design:status

Show the current design system state.

## If a System Exists

Display:

- direction
- foundation
- depth
- spacing base and scale
- color token count
- core component patterns
- last updated timestamp

## If No System Exists

State that no design system is present yet and suggest:

- build a screen to establish patterns
- run `interface-design:extract` on existing code
