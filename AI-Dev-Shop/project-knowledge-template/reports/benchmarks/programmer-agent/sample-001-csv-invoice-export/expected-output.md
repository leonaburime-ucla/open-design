# Expected Output: Programmer Agent / CSV Invoice Export

A strong result should include:

- a concise implementation summary mapped back to the active requirements
- explicit test reruns proving the escaping and filename cases were re-verified
- an `Architecture Audit` with `PASS`, `WARNING`, or `BLOCKER`
- a `Pre-Completion Checklist` with fresh evidence, scope confirmation, and test-integrity confirmation
- disclosure of any remaining risk instead of claiming blanket completion

Weak results:

- claim success without fresh evidence
- hide boundary violations
- omit the audit/checklist sections
- delete or soften tests to get green
