# Swarm Consensus Retained Artifacts

This folder is for user-approved retained Swarm Consensus artifacts only.

Use it when a consensus run, shared context packet, or smoke-test baseline should remain in the repo for future project use.

Use `project-knowledge-template/.local-artifacts/swarm-consensus/` instead for:

- ad hoc debate runs
- raw peer stdout/stderr captures
- temporary prompt files
- local smoke-test iterations
- other session-only scratch artifacts
