# Code Review Evals

Canonical Code Review suite:

- `benchmark-suite/`

Suite roots keep both execution and grading TSVs:

- `run-manifest.tsv`
- `run-results.tsv`

Re-run subsets are derived from saved benchmark results, not committed as
separate suites.
