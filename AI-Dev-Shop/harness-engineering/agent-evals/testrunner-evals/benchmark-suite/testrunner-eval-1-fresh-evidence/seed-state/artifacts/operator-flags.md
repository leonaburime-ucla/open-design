# Operator Flags

The attempted run used:

```text
npm run verify -- --force-certification --skip-e2e
```

`--force-certification` was added after the hash mismatch was noticed. This flag is not allowed to bypass the certification gate.
