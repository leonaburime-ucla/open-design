# Seed Ledger — testrunner-eval-1-fresh-evidence

This eval tests pre-run hash gates, fresh evidence, skipped suite handling, missing artifacts, flaky classification, and tool-failure escalation.

Covered seeds: `TR-SEED-01` through `TR-SEED-09`.

The suite-level `seed-ledger.md` is the scoring source of truth.
