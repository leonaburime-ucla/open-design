# Seed Ledger — testrunner-eval-2-coverage-artifacts

This eval tests coverage gate discipline, artifact merge behavior, per-file regression handling, and exemption judgment.

Covered seeds: `TR-SEED-10` through `TR-SEED-18`.

The suite-level `seed-ledger.md` is the scoring source of truth.
