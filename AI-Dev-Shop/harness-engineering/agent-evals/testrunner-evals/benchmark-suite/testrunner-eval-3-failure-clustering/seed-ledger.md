# Seed Ledger — testrunner-eval-3-failure-clustering

This eval tests evidence integrity, failure ownership, flake/state contamination handling, infrastructure classification, and read-only TestRunner behavior.

Covered seeds: `TR-SEED-19` through `TR-SEED-27`.

The suite-level `seed-ledger.md` is the scoring source of truth.
