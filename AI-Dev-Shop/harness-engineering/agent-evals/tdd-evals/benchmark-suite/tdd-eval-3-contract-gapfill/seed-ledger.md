# Seed Ledger — tdd-eval-3-contract-gapfill

This eval tests whether TDD performs disciplined gap fill instead of writing brittle, implementation-shaped, or no-spec tests.

Covered seeds: `TDD-SEED-19` through `TDD-SEED-27`.

The suite-level `seed-ledger.md` is the scoring source of truth.
