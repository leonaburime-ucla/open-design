# Seed Ledger — tdd-eval-1-spec-certification

This eval tests whether the TDD agent refuses uncertified or contradictory upstream inputs before writing tests.

Covered seeds: `TDD-SEED-01` through `TDD-SEED-09`.

The suite-level `seed-ledger.md` is the scoring source of truth.
