# Seed Ledger — tdd-eval-2-outcome-matrix

This eval tests whether TDD builds behavior-level outcome coverage and risk-ranked test planning instead of copying thin implementation-shaped plans.

Covered seeds: `TDD-SEED-10` through `TDD-SEED-18`.

The suite-level `seed-ledger.md` is the scoring source of truth.
