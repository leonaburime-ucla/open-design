# Original Request

Analyze this existing codebase before we add billing exports. Scope the review to `src/billing` and shared runtime code. Do not execute project scripts, install dependencies, or change files.
