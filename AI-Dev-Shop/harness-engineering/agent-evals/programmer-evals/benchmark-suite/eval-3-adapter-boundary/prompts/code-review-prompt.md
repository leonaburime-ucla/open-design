# Coordinator Dispatch — Code Review Agent

## Task

Programmer Agent has completed work on the payment gateway adapter. Review the implementation against the spec and all review dimensions.

## Spec Reference

Use `project-brief.md` as the active spec.

## Files to Review

All files in `src/` and `tests/`.

## Context

The Programmer was given an adapter wrapping a third-party payment SDK and asked to harden it for production. Assess whether error handling, timeout protection, logging, and security requirements are met.

## Output

Write findings to `eval-results/eval-results-run.md`. Include: model name and version used, findings with severity, spec compliance status, and function quality assessment.
