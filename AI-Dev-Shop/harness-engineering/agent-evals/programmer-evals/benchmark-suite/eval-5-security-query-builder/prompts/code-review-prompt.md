# Coordinator Dispatch — Code Review Agent

## Task

Programmer Agent has completed work on the secure search query builder. Review the implementation against the spec and all review dimensions.

## Spec Reference

Use `project-brief.md` as the active spec.

## Files to Review

All files in `src/` and `tests/`.

## Context

The Programmer was given a SQL query builder and asked to make it production-safe. Assess whether SQL injection prevention, input validation, PII protection, and all spec requirements are met.

## Output

Write findings to `eval-results/eval-results-run.md`. Include: model name and version used, findings with severity, spec compliance status, and function quality assessment.
