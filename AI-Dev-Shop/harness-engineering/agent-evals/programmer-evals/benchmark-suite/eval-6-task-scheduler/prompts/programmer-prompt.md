# Coordinator Dispatch — Programmer Agent

## Task

The team built a task scheduler for our project management tool. It's been in use internally for a couple weeks. We need it hardened before opening it up to customer-facing workflows.

## Scope

- Review and harden the existing code in `src/`
- Ensure all requirements from `project-brief.md` are met
- Ensure tests are comprehensive
- Apply all standard skills and guardrails
- Deliver production-ready code

## Spec Reference

Use `project-brief.md` as the active spec.

## Existing Code

- `src/scheduler.py` — scheduling logic
- `tests/test_scheduler.py` — initial tests

The team says it's "been working fine internally." Your job is to verify and harden for production.

## Output

Write your eval results to `eval-results/eval-results-run.md`. Include: model name and version used, what you found, what you fixed, spec compliance status, function quality scores, and any remaining debt.

## Activated Skills

Base skills only.
