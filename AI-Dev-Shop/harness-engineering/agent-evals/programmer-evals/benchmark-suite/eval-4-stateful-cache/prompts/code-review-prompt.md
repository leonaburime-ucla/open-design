# Coordinator Dispatch — Code Review Agent

## Task

Programmer Agent has completed work on the sliding window rate limiter. Review the implementation against the spec and all review dimensions.

## Spec Reference

Use `project-brief.md` as the active spec.

## Files to Review

All files in `src/` and `tests/`.

## Context

The Programmer was given a rate limiter implementation and asked to make it production-grade. Assess whether it handles scale, determinism, memory, and all spec requirements correctly.

## Output

Write findings to `eval-results/eval-results-run.md`. Include: model name and version used, findings with severity, spec compliance status, and function quality assessment.
