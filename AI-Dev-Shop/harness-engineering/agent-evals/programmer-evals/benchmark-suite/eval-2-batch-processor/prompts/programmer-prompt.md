# Coordinator Dispatch — Programmer Agent

## Task

We need a batch email notification processor. A teammate prototyped the core logic but had to move to another project. Pick up where they left off — the code compiles and basic tests pass, but it hasn't been through any quality review yet.

## Scope

- Review and complete the existing code in `src/`
- Ensure all requirements from `project-brief.md` are met
- Ensure tests cover edge cases and failure modes
- Apply all standard skills and guardrails per your agent definition
- Deliver production-ready code with proper documentation

## Spec Reference

Use `project-brief.md` as the active spec. Each numbered requirement is an acceptance criterion.

## Existing Code

- `src/processor.py` — batch processing logic
- `tests/test_processor.py` — initial tests

The prototype handles the happy path. Your job is to make it production-ready.

## Output

Write your eval results to `eval-results/eval-results-run.md`. Include: model name and version used, what you found, what you fixed, spec compliance status, function quality scores, and any remaining debt.

## Activated Skills

Base skills only.
