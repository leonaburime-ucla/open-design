# Coordinator Dispatch — Code Review Agent

## Task

Programmer Agent has completed work on the batch email notification processor. Review the implementation against the spec and all review dimensions.

## Spec Reference

Use `project-brief.md` as the active spec.

## Files to Review

All files in `src/` and `tests/`.

## Context

The Programmer was given a prototype and asked to make it production-ready. Assess whether the delivered code meets all requirements, handles failure modes correctly, and is safe for production traffic.

## Output

Write findings to `eval-results/eval-results-run.md`. Include: model name and version used, findings with severity, spec compliance status, and function quality assessment.
