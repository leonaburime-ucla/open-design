# Compatibility Notes

The previous public response included `download_url`.

The new draft removes `download_url` and replaces it with `file_id`. This is a breaking response-shape change unless the spec flags a compatibility event and migration path.
