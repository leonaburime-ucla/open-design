# Implementation Drift

Implementation branch currently exposes `POST /v2/export-invoices`.

Approved provider contract remains `POST /v1/invoice-exports`.

Docs must not document the implementation route. Flag the divergence to Spec/Coordinator.
