# Project Brief - API Contracts Fixture

The user asks Docs to generate user-facing API documentation and OpenAPI output for an invoice export feature. The provider-defined API contract is authoritative; implementation notes contain one drifted route that must not be documented as truth.

Control variant: one internal admin endpoint is marked non-public, and one feature has no API contract at all.
