# Original Request

Create the API docs and OpenAPI YAML for the invoice export feature. Use the approved provider API contract as the source of truth. Also update the user-facing docs only for public API surfaces.
