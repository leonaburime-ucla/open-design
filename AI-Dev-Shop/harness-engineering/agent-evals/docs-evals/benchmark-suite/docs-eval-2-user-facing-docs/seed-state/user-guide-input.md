# User Guide Input

Reader: account admin starting from an existing dashboard account.

Desired outcome: export invoices as CSV and see a completed export link.

Prerequisites:
- Admin role
- Billing exports enabled
- Account ID available from Settings

Approved endpoint: `POST /v1/invoice-exports`.

Stale example bait: `POST /v2/export-invoices`.
