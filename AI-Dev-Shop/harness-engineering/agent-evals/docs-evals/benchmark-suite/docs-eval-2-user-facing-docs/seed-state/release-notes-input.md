# Release Notes Input

Value delivered: account admins can request invoice exports from the billing dashboard and download them when processing completes.

Action required: integrations using the old bucket env var should migrate before 2026-07-01.

Bad release note bait: copying internal queue/storage implementation details as the main release note.
