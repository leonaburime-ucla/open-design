# Changelog

Immutable specs-as-built impact entries live here.

Use one file per spec or reverse-spec slice:

```text
<spec-id-or-run-id>-impact.md
```

Each entry should link to affected components and raw reverse-spec evidence. It should not own current function or module truth.
