/** @module core/constants
 * Shared brand constants used across extraction transcripts and kit previews.
 * Core constants import no sibling concern and are safe for every brands layer to depend on.
 */

/** Active browser-tab id used by brand extraction projects for the source site. */
export const BRAND_BROWSER_TAB_ID = '__browser__:1';
