/** @module generation/index
 * Provisional brand generation from deterministic harvested material.
 * Extraction imports this barrel to synthesize a valid brand before finalization.
 */

export * from './provisional.js';
