/** @module response/index
 * Response-side serialization layer: JSON body writes, the standard `ApiError` envelope, and
 * error-code -> HTTP-status mapping. No dependencies on other subdirectories.
 */
export * from './response.js';
