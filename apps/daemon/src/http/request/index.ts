/** @module request/index
 * Request-side normalization layer: raw Express `Request` -> `RouteInputContext`, and the
 * standard validation-error builder. Depends only on `core/`.
 */
export * from './parse.js';
